# AA の執筆を始めるチャットへ

このファイルは、**AA（Analysis and Approaches）の本を書き始めるチャットに、
最初に読ませるための引き継ぎ**です。

AI SL（39項目）と AI HL（39項目）は書き上がりました。
その 78 ページで固まった書き方と、失敗して直したところを、ここにまとめてあります。

---

## 0. 最初にすること（この順で）

1. **このファイルを最後まで読む。**
2. `_TEMPLATE.qmd` を読む。**本の全ルールはそちらが本体**です。
   このファイルは、そこに AI HL で足したことと、AA での違いを書き足したものです。
3. `tools/README.md` を読む（検査の小道具の使い方）。
4. `glossary-ai.qmd` の作り方を見る（AA では別ファイルを作ります。第 5 節）。
5. **書き上がったページを 1 本、通しで読む。**
   `ai-hl/03-geometry-and-trigonometry/ahl-3-13b.qmd` が、AI 版の完成形です。
   **語り口・例題の作り方・検算の書き方は、これを手本にしてください。**
   ただし **ページの並び順（`##` の構成）は AA で変えました。** 第 3 節が優先です。
   ・冒頭の「公式集の欄」ブロックは、AA では作りません
   ・冒頭の「シラバスが、この項目に求めていること」は、AA では末尾に折りたたみます
   ・`## Using your GDC` の節は、AA では原則置きません
6. `figs/ai-hl/check_ahl_3_13b.py` を読む。**検証のしかたの手本**です。
7. `_AA-SL-PLAN.md`（AA SL 60 ページの一覧）を読む。
8. 一覧で**次に空いているページ**から書く。第 1 節はもう決まっているので、聞き直さない。

---

## 1. 決まったこと（2026-09-07、AA SL 着手時）

第 1 節はもともと「ユーザーに聞くこと」でしたが、AA SL については答えが出ました。
**もう聞かないでください。** HL に残っている確認だけ、いちばん下にあります。

### (a) 書く順番

**AA SL を全項目書き切ってから、AA HL。**

### (b) 公式集

`2.Math AA SL formula booklet(New) copy.pdf`（10 ページ）がリポジトリ直下にあります。
中身は **SL の欄だけ**で、AHL の欄はありません。AA SL はこれで足ります。

**AA の公式集は AI のものとは別の文書です。記憶で書かないこと。**

### (c) シラバス

`5. (Subject guide) mathematics-analysis-and-approaches-guide-en ... copy 2.pdf`（74 ページ、
first assessment 2021）。SL・HL の両方が入っています。

### (d) ページの分け方

**原則 1 項目 1 ページ。** どうしても長くなる項目だけ a / b に分けます。
AA SL の 51 項目を 60 ページに割る計画が `_AA-SL-PLAN.md` にあります（9 項目を分割）。
**分けるかどうかの判断は、こちらでやってよい。** 分けたときに報告するだけで足ります。

### (e) ファイル名とアンカーの接頭辞

AI SL に `sl-1-1.qmd` があり `{#sec-sl-1-1}` を使っているので、**AA SL は `aasl-` を付けます。**

| もの | 書き方 |
|---|---|
| ファイル | `aa-sl/01-number-and-algebra/aasl-1-1.qmd` |
| 見出しのアンカー | `{#sec-aasl-1-1}` |
| ページ内の参照 | `@eq-aasl11-...`、`@exm-aasl11-...` |
| 図 | `figs/aa-sl/make_aasl_1_1.py` → `aa-sl/.../img/aasl-1-1-xxx.svg` |
| チェッカー | `figs/aa-sl/check_aasl_1_1.py` |

### ★ HL に入る前に、ユーザーに聞くこと

**AA HL の公式集（AHL の欄が入った PDF）をもらう。** 手元にあるのは SL 版だけです。

---

## 2. AA が AI と決定的に違うところ

`aa-sl/index.qmd` に、すでにこの分析が書いてあります。要点だけ:

### (a) Paper 1 が電卓なし

AI の本は、全ページに **`## Using your GDC (TI-Nspire CX II)`** の節があります。
AA では、その前提が崩れます。

- 「Using your GDC」の節を、**そのまま持ってくることはできません。**
- **例題・演習の数値を、手で解ける形に作り直す**必要があります。
- Common errors も、**手計算で起きる間違い**に書き直します。

**AA では、GDC の独立した節を原則として置きません。**
電卓が要る場面に、その場で折りたたみの callout を差し込みます（第 3 節）。
統計のように操作が長い項目だけ、例外として節を置きます。

### (b) 「なぜ」の比重が上がる

AA は証明・導出を要求します。AI では `Why it works` を短くしていましたが、
AA では**厚く書く**ことになります。ただし、

- **シラバスが `Not required` と書いていることは、書かない。**
  AI HL 3.13 では `Not required: generalized properties and proofs of scalar
  and cross product.` があったので、Why it works を「読まなくても解ける」と
  明記しました。AA でも同じ確認を、項目ごとにしてください。

### (c) AI SL と共通の 26 項目は、流用できない

項目番号（SL 1.1〜1.5, 2.1〜2.4, 3.1〜3.3, 4.1〜4.9, 5.1〜5.5）は一致しますが、
**AI SL のページをコピーしてはいけません。** 理由は `aa-sl/index.qmd` に書いてあります。

流用できるのは、数学そのものの説明の骨格くらいです。

### (d) 別プロジェクト `ibdp-math-book`（英語の AA HL 版）とは無関係

**そちらの記憶を流用しないこと。** この本は日本語で、読者が違います。

---

## 3. ページの形（★AA で変えました）

AI 版は、上から順に「できるようになること → 公式集の表 → シラバスの英語の引用 →
The idea」でした。**AA では、この 2 つ目と 3 つ目をやめます。**

理由は、生徒が上から読むからです。何の話かまだ分からない段階で公式集の表を見せると、
そこで読むのをやめます。シラバスの英語の引用は、実際には先生向けの情報です。

### 新しい順序

```
---
sidebar: aa-sl
---

# SL X.Y — <English title>（<日本語>） {#sec-aasl-x-y}

::: {.callout-note}
## What you should be able to do        ← 読む前の地図。ここだけは冒頭に残す
:::

## The idea                              ← ### 1. 〜 ### N. の連番。各節に {#anchor}
    ・公式が【初めて出た直後】に、公式集の話を差し込む
    ・電卓が要る場面に、その場で差し込む
## Why it works
## Worked examples                       ← 例題 4 本（重い項目は 5 本）
## Common errors                         ← callout-warning を 4〜6 個
[## Using your GDC (TI-Nspire CX II)]    ← ★統計など、操作が長い項目だけ
## Exercises                             ← 演習 10 問  ← ★ページはここで終わる
```

### (a) 公式集の話は、式が出た直後に差し込む

**ページ冒頭の「公式集の <項目> の欄」というブロックは、AA では作りません。**
代わりに、その式が本文に初めて出たところで、すぐ下にこう置きます。

```
::: {.callout-important}
## この式は公式集にあります
試験中に配られるので、**覚える必要はありません。** 覚えるのは、
どういうときにこれを使うかのほうです。
:::
```

- **載っていない**ものは、その場で「**公式集にありません。ここは覚えてください**」と書く。
  これは生徒にとっていちばん役に立つ情報なので、省かない。
- どちらなのかは、**必ず AA SL の公式集 PDF を見てから**書く。記憶で書かない。
- 末尾の「参考」ブロックに、公式集の該当欄の表を**もう一度は載せません**（1 回だけ）。
  末尾に置くのはシラバスの引用だけです。

### (b) 電卓は、要る場面にその場で差し込む

**AA は Paper 1 が電卓なし**なので、電卓は本筋ではなく補助です。
`## Using your GDC` の節は、原則として作りません。

```
::: {.callout-tip collapse="true"}
## Paper 2 では、電卓でこう出せます
menu → ...
:::
```

- **`collapse="true"` を必ず付ける。** 閉じた状態なら、読みの流れが切れません。
- 見出しは「**Paper 2 では、**」で始める。Paper 1 では使えないことが、毎回目に入ります。
- **例外：統計のように操作が長い項目**（SL 4.3・4.4・4.8・4.9・4.12 など、シラバスが
  `must be found using technology` と書いている項目）では、**Exercises の前に**
  `## Using your GDC (TI-Nspire CX II)` の節を置いてよい。長い手順を分断しないためです。
  その場合も、節の冒頭に「これは Paper 2 でだけ使えます」を置きます。

### (c) シラバスの引用は、ページに置かない（★2026-09-07 変更）

**末尾の「参考：この項目のシラバス（原文）」は、作りません。** 一度作ってお見せしたところ、
ユーザーから「この部分はいらない」と言われました。生徒向けの本に、先生向けの原文を
貼らない、という判断です。

そのかわり、**生徒に直接役立つ 1 文だけ**を、必要な節の本文に引用します。

- **`Not required:` の行**は、本文に書く。「ここまでは問われません」は、生徒の時間を守ります。
- **答案の書き方を縛る Guidance の一文**も、本文に書いてよい
  （SL 1.1 の `Calculator or computer notation is not acceptable.` がその例）。
- それ以外の Content 欄・Connections 欄・assessment の節は、**引用しない。**
  読んで得た中身は、日本語の説明として本文に溶かします。
- 引用するときは**逐語**。要約しない。

### (d) 図には、単位と「何の量か」を書く（★2026-09-07 追加）

`person 1.7` では、$1.7$ が何なのか伝わりません（実際に聞かれました）。

- ラベルは `person (height)`、`Earth (diameter)`、`atom (width)` のように、**何の長さかを付ける。**
- **単位は図の見出しに入れる**（`(a) Lengths in metres — ...`）。軸の端に小さく置くだけでは見落とされます。
- 本文でも、図を指すときに単位と読み方を $1$ 行書く。

### 数の決まり（AA 版）

| もの | 数 |
|---|---|
| `##` の見出し | **5**（The idea / Why it works / Worked examples / Common errors / Exercises） |
| 同上、GDC の節を置く項目 | **6** |
| 例題 `::: {#exm-...}` | **4**（重い項目だけ 5） |
| 演習 `[n]{.ex-no}` | **10** |
| `::: {.ex-sep}` | **演習数 − 1**（= 9） |
| `<details class="jp-trans">` | **例題数 + 演習数**（= 14） |
| 行頭の `---` | **2（front matter）+ 例題数**（= 6） |
| The idea の `### N.` | **1..N の連番** |
| GDC の `### N.`（置いた項目だけ） | **1..N の連番** |
| 末尾のシラバスのブロック | **置かない** |

`.model-answer` の数は**決まっていません**。
`Explain why` / `Justify` / `Comment on` / `Interpret` / `Identify the error`
が付いた設問に、**必ず** 1 つ置く、というだけです。

### `---` の位置

**例題では、日本語訳の `</details>` の直後に `---` を 1 本入れます。**
演習には入れません。だから `---` の総数が「2 + 例題数」になります。

### 演習の折りたたみは 3 つ

```
<details class="jp-trans">    日本語訳
::: {.callout-note collapse="true"}   ## 解答例（答案用紙に書くこと）
::: {.callout-tip collapse="true"}    ## 解説
```

**`.model-answer` は、演習では「解説」の中に入れ子にします。**
例題では、解説の流れの中に直に置きます。

---

## 4. 書き方の決まり（`_TEMPLATE.qmd` に、AI HL で足したもの）

### (a) 禁止語

誰でもできる／簡単です／当然／明らか／もちろん／当たり前／そのとおり

（`_TEMPLATE.qmd` にもありますが、AI HL の実作業で
**「当然」「明らか」「そのとおり」が何度もすり抜けました。**
書き終わったら必ず grep してください。）

### (b) 検算の見出しは `**検算。**` 一択

`**検算（(a) について）。**` のように括弧付きも可。
**「確かめ。」「確かめます。」を見出しに使わない。**

### (c) ★ 検算は、「それが捕まえるはずの誤り」で必ず試す

AI HL のレビューで**いちばん多く見つかった欠陥**がこれです。次の 3 つはすべて失格:

1. **循環している** — 同じ計算をやり直しているだけ
   （例: 単位ベクトルを $\frac{1}{L}$ 倍して「長さが 1」を確かめる。$L$ が
   何であっても 1 になるので、何も捕まえない）
2. **順序を保存している** — 誤りが同じ形で再現してしまう
   （例: 内積を「順番を入れかえて」出し直す。項の順は変わらないので、
   打ち消し合う 2 項で止める誤りは同じように起きる）
3. **右と左が一致する点で評価している**
   （例: $t=0$ で確かめる。そこでは正答と誤答が同じ値になる）

**「こう間違えたら、こうなる」と書いたら、その値を実際に計算して確かめること。**
チェッカー（第 6 節）に、その対比をそのまま書き残してください。

### (d) ★ 無条件の主張をしない

見出し・太字・表のセルは、**本文と同じかそれより厳しく**書きます。
AI HL では次がすべて誤りとして直りました。

- 「$\mathbf{a} \parallel \mathbf{b} \iff \mathbf{a} = k\mathbf{b}$」→ $\mathbf{a},\mathbf{b} \neq \mathbf{0}$ が要る
- 「$\mathbf{v}\cdot\mathbf{w} > 0$ なら鋭角」→ $\theta = 0^\circ$ は鋭角ではない
- 「$v_y = 0$ なら最高点」→ 加速度が下向きのときだけ
- 「$\mathbf{v}\times\mathbf{w}$ は両方に垂直」→ $\mathbf{0}$ のときは言えない
- 「$\overrightarrow{AB}$ と $\overrightarrow{BC}$ を使ってはいけない」
  → じつは $\overrightarrow{AB}\times\overrightarrow{BC} = \overrightarrow{AB}\times\overrightarrow{AC}$ で、同じ答えになる

**書いた主張ごとに、反例を作ろうとしてみてください。**

### (e) 丸めは最後の 1 回だけ

- 有効数字 3 桁で $121.33^\circ$ は **$121^\circ$**。$3$ 桁の角では小数が消えます。
- $180^\circ$ から引くときは、**丸める前の値から**引く
  （$109.65^\circ \to 110^\circ$ にしてから引くと $70^\circ$ になり、正しい $70.3^\circ$ と合わない）。

### (f) 公式集にあるものを「覚える」と書かない

試験中に配られるので、「思い出す」場面が存在しません。
公式集に**ない**ものは、「公式集にありません」と明記してよい（むしろ書くべき）。

### (g) 解答例・`.model-answer` は英語だけ

日本語は、ブロックの**外**に括弧で添えます。

### (h) 英語の用語は、そのページでの初出時に日本語訳を括弧で

**ページごとに**やり直します（生徒は 1 ページだけ読むことがあるので）。
英語が先、日本語が括弧。逆にしない。

### (i) 過去問を書かない

例題も演習も**すべて自作**です。IB の過去問を記憶から書くことは、絶対にしません。

---

## 5. リンクと記法の落とし穴（全部、実際に踏んだもの）

- ★ **ページをまたぐ `@exm-` `@eq-` `@fig-` `@tbl-` は動きません。**
  他ページを指すときは、**ふつうのマークダウンのリンク**にします。
  ページの中の `@`-ref は使えます。
- ★ **リンクに書く「第 N 節」は、相手のページの実際の番号と合わせる。**
  AI HL で 3 か所ずれていました。アンカーは合っているのに番号が違う、
  というのは grep では見つかりません。**相手のファイルを開いて数えてください。**
- ★ `✓` `✗` `①` `②` は KaTeX にメトリクスがないので、**数式の外に出す**。
- ★ **表のセルの中の `|` は、表を壊します。** `\lvert … \rvert` を使う。
- ★ `::: {` で始まる fence の**前には空行が要ります。**
- ★ 日本語の見出しには、**手で `{#anchor}` を書く**（自動 id が付きません）。
  英語の `##` 見出しには Quarto が自動で付けます（`#why-it-works` など）。
- ★ **コードスパンの中の数式は、そのまま文字として出ます。** 数式はバッククォートの外へ。
- ★ 折りたたみ（`<details>`）の中は、**空行で区切る**。空行がないと数式が生で出ます。

---

## 6. 検証のしかた（AI HL でやっていた手順、そのまま）

1 ページ書くごとに、この順で回します。**省略しないこと。**

### (1) 数値を sympy で出し直す

本文の式を写すのではなく、**第一原理から計算**します。
「こう間違えたら、こうなる」の誤答側も、実際に計算します。

### (2) 図を作り、PNG に落として、**目で見る**

```bash
python3 figs/aa-sl/make_xxx.py
node tools/svg2png.js /root/book/aa-sl/.../img/xxx.svg
# Read ツールで PNG を開いて、実際に見る
rm -f aa-sl/**/img/*.png
```

**必ず見つかること**（AI HL で全部起きました）:

- 矢印が別の矢印に**完全に隠れている**
- 図が**演習の答えを先に見せている**（例: 放物運動の図が演習の最高点と飛距離を印字していた）
- ラベルが線と重なる／枠からはみ出す
- 描いた角と、書いたラベルの角が違う

### (3) `mathcheck.js` と `mathwidth.js`

```bash
node tools/mathcheck.js  <file.qmd>   # 0 failed でなければ数式が出ない
node tools/mathwidth.js  <file.qmd>   # 700px を超える式はスマホで切れる
```

### (4) レビュー（opus のサブエージェント）

**これがいちばん効きます。** AI HL の 7 ページで、1 ページあたり 15〜22 件出ました。

サブエージェントに渡すもの:

- シラバスと公式集の**逐語**の事実（記憶で調べさせない）
- 決まっている設計判断（変えさせない）
- 検証済みの GDC の事実
- ページの決まり（第 3・4 節）
- **やらせること**:
  1. 全部の数値を sympy で独立に再計算し、丸めも確かめる
  2. **すべての検算を、それが捕まえるはずの誤りで試す**
  3. 見出し・太字・表の主張に**反例を作ろうとする**
  4. **図を PNG にして、実際に見る**
  5. ページをまたぐリンク先と**節番号**を全部確かめる
  6. 数の決まり（第 3 節）を数える
  7. シラバス・公式集の引用が逐語か
  8. GDC の記述が検証済みの事実と合うか

**指摘は全部適用してください。** 適用したら、第 6 節 (5) の見張りを置きます。

### (5) チェッカーを書く（`figs/<course>/check_xxx.py`）

`figs/ai-hl/check_ahl_3_13b.py` が手本です（OK 1205 / NG 0）。中身は:

- **記号のまま**の恒等式（公式集の式が本当に正しいか）
- 全部の数値を第一原理から
- **誤答側の値**も計算して、検算が本当に区別することを確かめる
- レビューで直した箇所に `not_in_text(...)` の**見張り**を置く
  （直したはずの文が復活したら落ちる）
- 数の決まり（例題 4・演習 10・節番号の連番・`---` の本数）
- ページ内アンカーが全部あるか／ページをまたぐ `@`-ref を使っていないか
- 図のスクリプトの数値と、本文の数値が合っているか
- **図が演習の答えを載せていないか**
- 登録先（`_quarto-draft.yml` / `index.qmd` / glossary）に入っているか

### (6) レンダーして、警告を読む

```bash
(setsid bash -c 'cd /root/book && quarto render --profile draft > /tmp/r.log 2>&1' </dev/null >/dev/null 2>&1 &)
sleep 400
tail -3 /tmp/r.log; grep -iE 'WARN|error' /tmp/r.log | sort | uniq -c
```

**まだ書いていないページへの前方リンクの警告だけが、許される警告**です。
`Unable to resolve crossref` が出たら、ページをまたぐ `@`-ref を使っています。

### (7) サイト全体のリンク検査

`_site/**/*.html` を舐めて、`href` の**ファイルとアンカー**が実在するかを見ます。
このセッションではインラインの Python で回していました
（`ahl-3-13b` の会話に完全なコードがあります。必要なら書き直してください）。

### (8) チェッカーを全部流す

```bash
for f in figs/*/check_*.py; do echo "== $(basename $f)"; python3 "$f" 2>&1 | tail -2; done
```

**1 本でも NG があれば、直してから納品**します。

---

## 6-B. 道具の置き場所（2026-09、AA SL 1.1 を書いたときに整えたもの）

このリポジトリは **eishi さんの Mac の中**にあります。作業は、原則そこで完結させます
（クラウドに持ち出すのは、下の 2 つだけ）。

### Mac 側（`device_bash`）にあるもの

| もの | 状態 |
|---|---|
| python3 + sympy + matplotlib | 入っています。チェッカーと図は、ここで動かします |
| node + npm | 入っています。`npm install katex` は通ります |
| quarto | **自分で入れました**：`$HOME/quarto-1.6.42/bin/quarto`（★ **linux-arm64** の tarball。amd64 を落とすと `deno: No such file or directory` になります） |
| playwright のブラウザ | **入りません**（`npx playwright install` が失敗）。`mathwidth.js` はクラウド側で走らせます |

`tools/*.js` の `require` はクラウドの絶対パス固定なので、**パスを書き換えた複製**を
`$HOME/tools-local/` に置いて使いました。

```bash
sed "s#/home/claude/.npm-global/lib/node_modules/#$HOME/node_modules/#g" \
    tools/mathcheck.js > $HOME/tools-local/mathcheck.js
node $HOME/tools-local/mathcheck.js aa-sl/01-number-and-algebra/aasl-1-1.qmd
```

### レンダー

**1 ファイルずつ**回します。複数ファイルを並べると `withBinaryFile: does not exist` で落ちます。

```bash
export PATH=$HOME/quarto-1.6.42/bin:$PATH
quarto render aa-sl/01-number-and-algebra/aasl-1-1.qmd --profile draft
```

プレビュー（1 ファイルで完結する HTML。`*プレビュー.html` は `.gitignore` 済み）:

```bash
quarto render <file> --profile draft -M embed-resources:true
cp _site/.../xxx.html "AASL1-1_プレビュー.html"
# ★ そのあと、ふつうの render をもう一度かけて _site を元に戻すこと
```

`cdnjs.cloudflare.com` の 403 が出ますが、これは MathJax の polyfill だけなので**無視して構いません**。

### 図を目で見る手順（★ここが変わりました）

`tools/svg2png.js` は Mac 側では動きません（playwright のブラウザがない）。
代わりに、**`make_*.py` に PNG も書かせます**。

```bash
FIG_PNG=1 python3 figs/aa-sl/make_aasl_1_1.py     # SVG と PNG
cp .../img/aasl-1-1-scale.png .../img/_v1.png     # ★別名にコピー
# _v1.png を device_stage_files で上げて、Read ツールで見る
rm -f .../img/*.png                               # 見終わったら消す
```

★ **同じパスを 2 回 stage すると、古い内容が返ってくることがあります。**
見るたびに `_v1`、`_v2` と**別名**にしてください。

### 落とし穴（実際に踏んだもの）

- **`rm` は既定で禁止**されています（`Operation not permitted`）。
  `device_request_delete_permission` でフォルダごと許可をもらってください。**セッション中は 1 回で足ります。**
- **`device_commit_files` で同じパスに 2 回目を書いても、反映されないこと**がありました。
  **書き換えは `device_bash` の python でその場でやる**のが確実です（新規ファイルの 1 回目だけ commit を使う）。
- サブエージェントに原稿を見せるときは、`_r2.qmd` のように**別名でコピー**してから stage します（上と同じ理由）。

---

## 6-C. 検算の作り方（★2026-09-07、Topic 1 を書き上げて分かったこと）

opus のレビューで、**いちばん多く指摘されたのが検算**でした。以下は全部、実際に直したものです。

### (a) 効かない検算の $5$ つの型

| 型 | 例 | なぜ効かないか |
|:--|:--|:--|
| **同じ順序でやり直す** | $\dfrac{24}{3} = 8$ を出したあと、検算でも $\dfrac{24}{3}$ を計算する | 同じ間違いが同じように出る |
| **示したいことを使う** | $a^{0} = 1$ を示したあと、$2^{0} = 1$ で確かめる | 循環 |
| **一般化のもとにした値で確かめる** | $\dfrac{1}{6}-\dfrac{1}{7}$ から作った式を $n=6$ で確かめる | 作り方から、必ず合う |
| **誤答も同じ値になる点で代入する** | $a = 1$、$x = 1$、$x = 0$ | $1$ は何乗しても $1$。指数の誤りが消える |
| **範囲を絞るだけ** | 「$S_\infty$ より小さいので向きが合っています」 | $27$ でも $54$ でも通ってしまう |

### (b) 効く検算の型

- **別の道すじで、同じ値に着く。** 指数法則を使わずに値で出す、公式ではなく $1$ 項ずつ足す、
  など。**「$\ldots$ を使わない別の道すじです」と、何を使っていないかを書く。**
- **答えから逆に戻す。** $r$ を出したら、もとの式に入れ直して与えられた値に戻るか。
- **無限級数なら「残りをまとめて足す」。** $S_{\infty} = S_{n} + \dfrac{u_{n+1}}{1-r}$ は
  **ぴったり一致する**ので、「小さいから合っていそう」より強い。SL 1.8 で $4$ 回使いました。
- **問題文に戻って照合する。** $u_1$ と $u_2$ を取りちがえた誤りは、部分和では見つかりません。
  書き出した数列の $2$ 項目が問題文と合うかを見ます。
- **$r$ が負の等比級数は、はさみうち。** $S_{\infty}$ は連続する $2$ つの部分和のあいだにあります。

### (c) 効かないときは、効かないと書く

**「これは検算になりません」と書くほうが、弱い検算を載せるより価値があります。** 例：

- 「指数の和が $n$ になるのは、一般項が $a^{n-r}b^{r}$ の形をしている以上、いつもそうです。検算にはなりません。」
- 「定数項は $b^{n}$ そのものなので、値の確認にはなりません。確認できるのは**位置**です。」
- 「$x = 1$ を入れる検算では、`ascending` の順序のまちがいは見つかりません。」

### (d) 演習の答えを、本文と例題に書かない

Topic 1 で $7$ 件見つかりました。**本文で説明に使う数と、例題・演習の数は、必ず変える。**
チェッカーに `not_in_text` の見張りを置くこと。

- SL 1.9 では、本文のパスカルの三角形を $n = 4$ で止めました（演習 $1$ が $n=5$、演習 $8$ が $n=6$）。
- SL 1.7b では、例題 $4$ 問すべてが本文で解かれていました。本文側の数を全部変えました。

### (e) 条件の抜け

$a \neq 0$、$a \neq 1$、$b \neq 1$、$x > 0$、分母 $\neq 0$、$0 \leq r \leq n$。
**「いつも成り立つ」と書く前に、反例を $1$ つ探す。** Topic 1 で直した例：

- 「指数法則が使えるのは底が同じときだけ」→ 積の累乗 $(ab)^n$ は底がちがっても使える
- 「項が小さくなるので収束する」→ 調和級数
- 「$u_1$ は $S_\infty$ より小さいはず」→ $r < 0$ なら逆になりうる
- 「符号は $1$ つおきに変わる」→ $a$ と $b$ の符号が逆のときだけ
- 「$\log_a(x+y) \neq \log_a x + \log_a y$」→ $x+y=xy$ のときは等しい

### (f) チェッカーの決まりごと

- `FIGSTR` からは **色コード（`#` で始まる文字列）を外す。** 外さないと `"#b45309"` の
  `45` や `30` が「演習の答えの漏れ」として誤検出されます。
- `\$`（通貨）は、$ の区切りとして数えない。`TEXT.replace("\\$", "")` してから
  `✓/✗` を探すこと（`tools/mathcheck.js` と同じ扱い）。
- `sp.solve` は複素数解も返す。**`if v.is_real` で絞る**こと（$9^x = 27$ で踏みました）。
- 負の $r$ の `sp.limit` は `NotImplementedError` になる。`abs(r)**n` の極限で代用する。
- 表示数式の中に `@eq-...` を書かない。Quarto が解決せず、そのまま印字されます。
  チェッカーは `TEXT.split("$$")` の奇数番目を見て検出します。
- `_ticked` の正規表現は `SL [0-9.]+[ab]?` にする（1.7a / 1.7b があるため）。

---

## 6-C-2. 断定を避ける言い方（★2026-09-07、ユーザー指示）

### (a) 数学的に「常に無効」と読める書き方をしない

**「両辺を同時に変形してはいけない」は、数学としては言い過ぎ**でした。すべての変形が
**同値変形**（逆向きにも成り立つ変形）なら、論理としては通ります。書くべきなのは、

- **IB の答案では、LHS → RHS（または逆向き）が最も安全**であること
- 両辺を同時に変形するなら、**一つひとつが逆向きにも成り立つかを確かめる必要**があること
- $2$ 乗のように**戻せない操作**を使うと、必要な向きの論理が言えないこと

の $3$ つです。SL 1.6 は、この形に書き直しました。

### (b) 採点の断定は弱める

**シラバスから直接確かめられる規則は、そのまま残す。**
（例：`Calculator or computer notation is not acceptable`）

**markscheme 次第で変わるものは、弱める。**

| 前 | 後 |
|:--|:--|
| 得点になりません | 得点にならない可能性があります |
| 点にはなりません | 示したことになりません／必要な過程を示したことになりません |
| 点を落とします | 得点にならないことがあります |
| 点を半分落とします | 得点が半分になることがあります |
| 方法点がもらえません | 方法点を得るためにも、途中式を書いておくのが安全です |
| ここが得点になります | この条件の確認が採点対象になることがあります |

### (c) 割り算・比を定義したら、分母の条件を書く

$r = \dfrac{u_2}{u_1}$ のような定義の直後に、**分母になる項が $0$ でないこと**を一言。
見出しも「数列が決まる」のような断定ではなく、「$r$ と $u_1$ を求められる」にします。

### (d) 発散の理由は、$r < -1$ でも通る言い方にする

「大きな数を足し続けるから」は $r > 1$ のイメージにしか合いません。**項の絶対値**と、
**部分和の変わり方が $0$ に近づかないこと**で述べます。$r < -1$ の例（$3-6+12-24+\cdots$、
部分和 $3, -3, 9, -15, \ldots$）を $1$ つ添えると伝わります。$u_1 \neq 0$ の但し書きも。

### (e) 用語は $1$ つに決める

`growth factor` は**増加倍率**（$1.1$）、`growth rate` は**増加率**（$10\%$）。
併記や言いかえをせず、対訳表にも両方入れて区別を書きます。

### (f) 読み方の案内は、入口ページに $1$ 回だけ

各ページの冒頭に機械的に足すと、かえって読みにくくなります。`aa-sl/index.qmd` の
「この本の読み方」に置きました。

## 6-D. device_commit_files の落とし穴（もう一度）

**同じ `stagedPath` を $2$ 回コミットすると、$1$ 回目の内容が書かれます。**
パッチスクリプトを直して再送するときは、**クラウド側のファイル名も変える**こと
（`p17a.py` → `p17a_v2.py`）。devicePath だけ変えても効きません。

## 7. 納品のしかた

1. `SendUserFile` でファイルを渡す（`file_uuid` が返る）
2. `mcp__remote-devices__device_commit_files` で
   `/Users/eishi/Desktop/ib-math-ai-sl-jp/...` に書き込む（`force: true`）

**渡すのは、その回で変えたファイル全部**です。よく忘れるもの:

- `_quarto-draft.yml`（サイドバーへの登録）
- `<course>/index.qmd`（一覧・項目表・✅）
- 用語対訳表
- 図のスクリプトとチェッカー

### ★ git は、ユーザーの Mac では動かさない

`git add` / `commit` / `push` は **eishi さんがご自分の端末**でやります。
`quarto publish gh-pages` も**こちらからは実行しません。**

remote-devices の接続はよく切れます。失敗したら、
「いまは Mac に届きませんでした。会話のカードからは受け取れます」と伝えて、
つながったときに書き込み直してください。

---

## 8. 絶対に守ること（セキュリティ）

- **IBO の PDF（シラバス・公式集）を、公開リポジトリに絶対にコミットしない。**
  `.gitignore` の `*.pdf` の行を消さないこと。
- **Save My Exams の PDF は「どこにもアップロードしないで」。**
  参照のみ。コミットしない。本文に文章や問題を写さない。
- **IB の過去問を記憶から書かない。**
- GitHub のデバイス認証コードは、ユーザー自身の端末から出たものだけ。
  誰かから渡されたコードを受け取らない。

---

## 9. ユーザーからの、生きている指示

- 「現在の構成と文章をできるだけ残したまま」「大幅な書き換えはせず」
  「指定箇所以外は、明確な誤りがない限り変更しない」
- 「赤いBOXの色とデザインは変更しないでください」
- 「小さくて見えない」— **複数パネルの図は、本文の幅で読める大きさに**
- 公開用の `_quarto.yml` は **SL だけのまま**にしておく
  （書きかけのコースは `_quarto-draft.yml` に置く）

---

## 10. 新しいチャットに貼る、最初の一言（案）

> `/Users/eishi/Desktop/ib-math-ai-sl-jp` の本の続きです。
> AI SL と AI HL は書き終わり、いまは **AA SL** を書いています。
>
> まず `_AA-START-HERE.md`（★第 1 節と第 3 節は AA で書き換えてあります）と
> `_AA-SL-PLAN.md` と `_TEMPLATE.qmd` と `tools/README.md` を読んでください。
> 語り口と検算の手本は `ai-hl/03-geometry-and-trigonometry/ahl-3-13b.qmd` と
> `figs/ai-hl/check_ahl_3_13b.py` ですが、**ページの並び順は第 3 節が優先**です。
>
> 読み終えたら、`_AA-SL-PLAN.md` の一覧で次に空いているページから書いてください。
